% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsco.R
\name{tsco}
\alias{tsco}
\title{Fit a Two-Stage Conditional Odds (TSCO) Model}
\usage{
tsco(
  formula,
  data,
  cutoff_level,
  levels = NULL,
  stage1 = c("po", "multinomial"),
  stage2 = c("multinomial", "po"),
  po.reverse = TRUE,
  weights = NULL,
  na.action = stats::na.omit,
  warn_degenerate = TRUE,
  ...
)
}
\arguments{
\item{formula}{Model formula, e.g. y ~ x1 + x2 for individual-level data or
cbind(y0, y1, y2) ~ x1 + x2 for grouped count data.}

\item{data}{A data.frame.}

\item{cutoff_level}{The first outcome level in the upper partition, supplied
as a level label.}

\item{levels}{Optional character vector specifying the ordinal outcome level
order. For individual-level data, observed outcome values must be contained
in \code{levels}. For grouped count data with named cbind() columns, \code{levels}
must contain exactly the response column names and is used to reorder the
columns into ordinal order. For grouped count data without column names,
\code{levels} is assumed to describe the existing cbind() column order.}

\item{stage1}{Model for the conditional lower partition Y | Y < C.
Either "po" for proportional odds or "multinomial".}

\item{stage2}{Model for the collapsed marginal outcome {Y < C, C, ..., K - 1}.
Either "multinomial" or "po".}

\item{po.reverse}{Logical. Passed to VGAM::cumulative(reverse = ...).
The default TRUE matches the manuscript's parameterization of Pr(Y >= k | X).}

\item{weights}{Optional numeric case weights.}

\item{na.action}{Missing-data action passed to model.frame().}

\item{warn_degenerate}{If TRUE, warn when K = 3, which reduces to two binary regressions.}

\item{...}{Additional arguments passed to VGAM::vglm().}
}
\value{
An object of class "tsco".
}
\description{
Fits a two-stage conditional odds model for an ordinal outcome using
VGAM::vglm().
}
\details{
The outcome is partitioned at a user-specified cutoff C.
Stage 1 models Y | Y < C. Stage 2 models the collapsed outcome
{Y < C, C, ..., K - 1}.

The response may be either an individual-level ordinal outcome or a grouped
count response supplied using cbind(), as in
cbind(normal, mild, severe) ~ x.
}
