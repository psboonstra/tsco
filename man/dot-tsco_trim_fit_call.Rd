% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-tsco.R
\name{.tsco_trim_fit_call}
\alias{.tsco_trim_fit_call}
\title{Replace bulky literals in a fitted stage's recorded call.}
\usage{
.tsco_trim_fit_call(fit, fun = NULL, family = NULL)
}
\arguments{
\item{fit}{A fitted stage model.}

\item{fun}{A call or name to record as the fitted call's function.}

\item{family}{A replacement expression for the recorded \code{family} argument.}
}
\description{
\code{do.call()} splices the stage data frame, the case weights and the family
object into the call each fitter records, which inflates every fitted
\code{tsco} object by roughly the size of the analysis data. Nothing in the
package re-evaluates those recorded calls -- likelihood-ratio refits build
fresh calls and prediction goes through \code{newdata} -- so the literals are
replaced with placeholder symbols after fitting.
}
