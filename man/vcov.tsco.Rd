% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcov-tsco.R
\name{vcov.tsco}
\alias{vcov.tsco}
\title{Variance-covariance matrix for a TSCO model}
\usage{
\method{vcov}{tsco}(object, stage = c("both", "stage1", "stage2"), ...)
}
\arguments{
\item{object}{An object of class \code{"tsco"}.}

\item{stage}{Which stage to extract: \code{"both"}, \code{"stage1"}, or \code{"stage2"}.}

\item{...}{Ignored.}
}
\value{
A variance-covariance matrix. If \code{stage = "both"}, the returned
matrix is block diagonal with stage-specific coefficient names prefixed by
\code{"stage1:"} and \code{"stage2:"}.
}
\description{
Reported on the same sign-corrected scale as \code{coef.tsco()}. If a stage's
coefficients required a sign flip to match the manuscript's
parameterization (see \code{.tsco_sign_vector()}), the covariance matrix is
transformed as \verb{V_new = D V_old D}, where \code{D} is the diagonal +-1 sign
matrix -- not simply negated -- so that cross terms between flipped and
unflipped coefficients (e.g. an intercept and a slope) remain correct.
}
