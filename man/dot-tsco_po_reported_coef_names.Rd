% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-tsco.R
\name{.tsco_po_reported_coef_names}
\alias{.tsco_po_reported_coef_names}
\title{Reported coefficient names for a proportional-odds stage.}
\usage{
.tsco_po_reported_coef_names(
  coef_names,
  response_levels,
  n_thresholds,
  design_names = NULL,
  collapsed_label = NULL,
  cutoff_level = NULL
)
}
\arguments{
\item{coef_names}{The backend's coefficient names.}

\item{response_levels}{The levels of the stage's response variable.}

\item{n_thresholds}{The number of threshold (intercept) coefficients, which
come first in both backends' coefficient vectors.}

\item{design_names}{Design-matrix column names for the slopes, excluding
the intercept. Optional; raw names are kept when absent.}

\item{collapsed_label, cutoff_level}{The stage-2 collapsed level's internal
label and the cutoff level it stands for, so that its threshold can be
reported as \code{Y<C}. Optional.}
}
\description{
\code{coef.tsco()} reports PO thresholds on the lower-tail scale
\verb{Pr(Y <= level_j | X) = expit(gamma_j + x'beta)}, so the thresholds are
labelled by the level they bound rather than by the backend's own
convention. \code{rms::orm()} names its thresholds for the \emph{upper} tail
(\code{y>=2}), which contradicts the reported value once the sign correction in
\code{.tsco_sign_vector()} has been applied. Slopes take the design-matrix
column names, which \code{VGAM::vglm()} already uses, so that both engines
produce identical names for the same model.
}
