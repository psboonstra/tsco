% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-tsco.R
\name{.tsco_stage_diagnostics}
\alias{.tsco_stage_diagnostics}
\title{Convergence and separation diagnostics for one fitted stage.}
\usage{
.tsco_stage_diagnostics(
  fit,
  engine,
  coef_table = NULL,
  max_abs_coef = 10,
  max_se = 50
)
}
\arguments{
\item{fit}{A fitted stage model.}

\item{engine}{The stage fitting backend, either "orm" or "vglm".}

\item{coef_table}{A stage coefficient table from \code{.tsco_coef_table()}.}

\item{max_abs_coef, max_se}{Thresholds above which a coefficient is treated
as separated.}
}
\value{
A list with \code{converged}, \code{separated}, and \code{messages}.
}
\description{
Complete or quasi-complete separation drives maximum-likelihood estimates
and their standard errors toward infinity. The fitters usually still
return an object, so the resulting Wald statistics look ordinary while
being meaningless. This helper flags the two symptoms that matter in
practice -- a fit the backend reports as failed, and coefficients whose
magnitude and standard error are both implausibly large -- so that
\code{summary.tsco()} can say so rather than printing the numbers unannotated.
}
