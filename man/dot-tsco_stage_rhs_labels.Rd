% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-tsco.R
\name{.tsco_stage_rhs_labels}
\alias{.tsco_stage_rhs_labels}
\title{Rebuild a formula right-hand side from model-frame column names.}
\usage{
.tsco_stage_rhs_labels(tt)
}
\arguments{
\item{tt}{A terms object for the user's original formula.}
}
\value{
A character vector of term labels for the stage formulas.
}
\description{
\code{tsco()} fits its two stages against data frames whose columns are the
\emph{evaluated} model-frame columns, so a column produced by \code{log(x)} is named
\code{log(x)} and the variable \code{x} no longer exists. Reusing the original
right-hand side would therefore fail to resolve. Rebuilding each term from
the backticked model-frame names lets inline transformations such as
\code{log(x)}, \code{poly(x, 2)} and \code{rms::rcs(x)} work, while leaving formulas made
only of plain variables completely unchanged.
}
